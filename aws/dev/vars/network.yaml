vpc:
  cidr: "10.10.0.0/16" # Mandatory: The CIDR block for the VPC.
  tags:
    Name: "dev-vpc"
    Environment: "dev"
subnets:
  app_subnet:
    cidr_block: "10.10.1.0/24" # Mandatory: The CIDR block for the subnet.
    availability_zone: "us-east-1a" # Mandatory: The Availability Zone for the subnet.
    tags:
      Name: "dev-app-subnet"
  # db_subnet:
  #   cidr_block: "10.10.2.0/24"
  #   availability_zone: "us-east-1b" # Specify AZ
  #   tags:
  #     Name: "dev-db-subnet"