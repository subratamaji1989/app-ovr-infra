# Defines S3 buckets for the application.

# Bucket Definitions: A map of S3 buckets.
buckets:
  app_data_bucket:
    # Bucket names must be globally unique.
    name: "dev-app-data-bucket-unique-12345" # <-- CHANGE TO A GLOBALLY UNIQUE NAME
    tags:
      Name: "dev-app-data-bucket"
      Environment: "dev"